{
  "name": "yacd-android",
  "version": "1.0.0",
  "description": "yacd (Yet Another Clash Dashboard) Android App - built with Capacitor",
  "private": true,
  "scripts": {
    "build:yacd": "bash scripts/build-yacd.sh",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "build:apk": "npm run build:yacd && npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
